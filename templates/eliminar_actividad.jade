extends base
block titulo
    | Eliminar
block itemsNavbarBreadcrumb
    li
        a(href="/") Inicio
    li
        a(href="/inventario/") Inventario
    li(class="active") Eliminar
block body_content
    div(class="col-xs-10 col-xs-offset-1 col-sm-4 col-sm-offset-4 col-md-4 col-sm-offset-4 col-lg-4 col-lg-offset-4 instruction")
        form(action="",method="POST")|{% csrf_token %}
            h3 Vas a eliminar la actividad: {{ object }}
            h3 esta accion eliminara tambien el registro de la actividad en los calendarios, estas seguro?
            button(type="submit",class="btn btn-danger btn-block") Aceptar